# Neon PostgreSQL connection string
# Get your connection string from https://neon.tech
#
# For Vercel deployment, use the pooled connection URL:
# NEON_DATABASE_URL=postgresql://<user>:<password>@<host>/<database>?sslmode=require
#
# Example:
# NEON_DATABASE_URL=postgresql://neondb_owner:xxxx@ep-xxx-pooler.aws.neon.tech/neondb?sslmode=require
NEON_DATABASE_URL=postgresql://user:password@ep-example-pooler.aws.neon.tech/neondb?sslmode=require
PORT=5000

# Vercel Blob Storage token for image uploads
# Get your token from Vercel Dashboard > Storage > Blob
BLOB_READ_WRITE_TOKEN=your_vercel_blob_token_here

# ============================================================================
# GOOGLE OAUTH AUTHENTICATION
# ============================================================================
# Get your Google OAuth credentials from Google Cloud Console:
# 1. Go to https://console.cloud.google.com/
# 2. Navigate to "APIs & Services" > "Credentials"
# 3. Create OAuth 2.0 Client ID (Web application)
# 4. Configure Authorized JavaScript origins and Redirect URIs
#
# IMPORTANT: This Client ID must match VITE_GOOGLE_CLIENT_ID in frontend/.env
# The backend uses this to validate JWT tokens from Google OAuth
GOOGLE_CLIENT_ID=your-google-oauth-client-id.apps.googleusercontent.com

# Authentication bypass for development (NOT for production)
# Set to 'true' ONLY in local development to bypass authentication
# The backend will reject this setting in production environments
AUTH_DISABLED=false

# Redis Configuration
# Redis connection URL for caching (optional)
# Example: redis://localhost:6379 or redis://user:password@host:port
REDIS_URL=

# ============================================================================
# LOGGING
# ============================================================================
# Log level for application logs
# Options: error, warn, info, debug, trace
# Default: 'debug' in development, 'info' in production
LOG_LEVEL=debug

# ============================================================================
# EMAIL CONFIGURATION (OPTIONAL)
# ============================================================================
# SMTP configuration for sending email notifications
# Leave these empty if you don't want to send emails
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@order-management.local

# ============================================================================
# DIGEST JOB CONFIGURATION (OPTIONAL)
# ============================================================================
# Security secrets for scheduled digest email jobs
# DIGEST_JOB_SECRET: Custom secret for triggering digest jobs
# CRON_SECRET: Vercel Cron secret (auto-generated by Vercel)
# Leave empty if not using digest email feature
DIGEST_JOB_SECRET=
CRON_SECRET=
