# API Configuration
# Backend API URL - should point to your backend server
NEXT_PUBLIC_API_URL=http://localhost:5000/api

# NextAuth Configuration
# IMPORTANT: This must match your application URL exactly
# For local development: http://localhost:3000
# For production: https://yourdomain.com
NEXTAUTH_URL=http://localhost:3000

# NextAuth Secret - used to encrypt JWT tokens
# Generate with: openssl rand -base64 32
# IMPORTANT: Keep this secret and never commit to version control
NEXTAUTH_SECRET=your-secret-here-generate-with-openssl

# Google OAuth Configuration
# Get these from Google Cloud Console:
# 1. Go to https://console.cloud.google.com/
# 2. Create a new project or select existing
# 3. Enable Google+ API
# 4. Create OAuth 2.0 credentials
# 5. Add authorized redirect URIs:
#    - For local: http://localhost:3000/api/auth/callback/google
#    - For production: https://yourdomain.com/api/auth/callback/google
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Optional: Disable authentication for development
# WARNING: Only use in development, never in production
# AUTH_DISABLED=true
