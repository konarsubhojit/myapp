# API URL for the backend service
# In production, set this to your deployed backend URL (e.g., https://your-backend-app.vercel.app/api)
# If not set, defaults to http://localhost:5000/api
VITE_API_URL=http://localhost:5000/api

# Google OAuth Configuration
# Get this from Google Cloud Console > APIs & Services > Credentials
VITE_GOOGLE_CLIENT_ID=your_google_client_id_here

# Customer Feedback App URL
# Set this to the URL where the customer-facing feedback app is deployed
# If not set, defaults to http://localhost:3001
VITE_FEEDBACK_APP_URL=http://localhost:3001
