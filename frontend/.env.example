# API URL for the backend service
# In production, set this to your deployed backend URL (e.g., https://your-backend-app.vercel.app/api)
# If not set, defaults to http://localhost:5000/api
VITE_API_URL=http://localhost:5000/api

# Google OAuth Configuration
# Get this from Google Cloud Console > APIs & Services > Credentials
# IMPORTANT: Configure Authorized Redirect URIs in Google Cloud Console:
#   - For local dev: http://localhost:5173
#   - For production: https://your-app.vercel.app (or your deployed URL)
# The redirect URI must exactly match your application URL
VITE_GOOGLE_CLIENT_ID=your_google_client_id_here

# Customer Feedback App URL
# Set this to the URL where the customer-facing feedback app is deployed
# If not set, defaults to http://localhost:3001
VITE_FEEDBACK_APP_URL=http://localhost:3001

# Rollbar Error Tracking
# Get this from Rollbar Dashboard > Settings > Project Access Tokens
# This is the post_client_item token for error tracking in the browser
# If not set, Rollbar will be disabled
VITE_ROLLBAR_ACCESS_TOKEN=your_rollbar_access_token_here
