# API URL for the backend service
# In production, set this to your deployed backend URL (e.g., https://your-backend-app.azurewebsites.net/api)
# If not set, defaults to http://localhost:5000/api
VITE_API_URL=http://localhost:5000/api

# Microsoft Entra ID (Azure AD) Configuration
# Get these from Azure Portal > App Registrations > Your App
# VITE_AZURE_TENANT_ID can be 'common' for multi-tenant apps, or your specific tenant ID
VITE_AZURE_TENANT_ID=common
VITE_AZURE_CLIENT_ID=your_azure_client_id_here

# Redirect URI (optional - defaults to window.location.origin)
# VITE_REDIRECT_URI=http://localhost:5173
